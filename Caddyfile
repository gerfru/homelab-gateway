{
	default_sni home.lab
}

(security_headers) {
	header {
		X-Content-Type-Options nosniff
		X-Frame-Options DENY
		Referrer-Policy strict-origin-when-cross-origin
		Permissions-Policy "camera=(), microphone=(), geolocation=()"
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		-Server
	}
}

# Niles AI (home.lab + MagicDNS alias for Google OAuth)
niles.home.lab, ${NILES_TS_HOSTNAME} {
	tls internal
	import security_headers
	log {
		output stdout
		format json
		level INFO
	}
	reverse_proxy niles_core:8000
}

# PulseBase
garmin.home.lab {
	tls internal
	import security_headers
	log {
		output stdout
		format json
		level INFO
	}
	reverse_proxy pulsebase-api:8000
}

# Vikunja (Todos)
vikunja.home.lab {
	tls internal
	import security_headers
	log {
		output stdout
		format json
		level INFO
	}
	reverse_proxy vikunja:3456
}

# Evolution API (WhatsApp)
whatsapp.home.lab {
	tls internal
	import security_headers
	log {
		output stdout
		format json
		level INFO
	}
	reverse_proxy evolution_api:8080
}

# Uptime Kuma (Status Page)
status.home.lab {
	tls internal
	import security_headers
	log {
		output stdout
		format json
		level INFO
	}
	reverse_proxy gateway-uptime:3001
}

# Grafana (Logs)
logs.home.lab {
	tls internal
	import security_headers
	log {
		output stdout
		format json
		level INFO
	}
	reverse_proxy gateway-grafana:3000
}
